'use client';

import Image from "next/image";

export default function Home() {
    return (
        <main className="flex items-center justify-center h-screen m-0">
            <div>You have loaded into the wrong URL.</div>
        </main>
    );
}
